@model TutorGet.Models.UserEvent

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Bookmark Event</h2>
@if (ViewBag.TheResult == true)
{
    <div id="myAlert" class="alert alert-success">
        This event has been bookmarked successfully!
    </div>
}

@if (ViewBag.TheResult == false)
{
    <div id="myAlert" class="alert alert-danger">
        This event has already been bookmarked!
    </div>
}


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    if (ViewBag.TheResult == null)
    {

        <div class="form-horizontal">
            <h4>Do you want to bookmark this event?</h4>
            <hr />

            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.UserId)
            @Html.HiddenFor(model => model.EventId)
            
           @* <div class="form-group" >
                    @Html.LabelFor(model => model.EventId, "Event", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.DropDownList("Event", null, htmlAttributes: new { @class = "form-control", @readonly = "readonly"})
                        @Html.ValidationMessageFor(model => model.EventId, "", new { @class = "text-danger" })
                    </div>
                </div>*@

        <div class="form-group">
            @*<div class="col-md-offset-2 col-md-10">*@
            <div class="col-md-12">
                <input type="submit" value="Confirm" class="btn btn-default" />
            </div>
        </div>
        </div>
    }
}

    <div>
        @Html.ActionLink("Back to Event List", "Index", "Events")
    </div>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
