@model TutorGet.Models.UserEvent

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Remove From Event Bookmark </h2>
@if (ViewBag.TheResult == true)
{
    <div id="myAlert" class="alert alert-success">
        This event has been removed successfully!
    </div>
    <div>
        @Html.ActionLink("Back to My Bookmark", "Index")
    </div>

}

@if (ViewBag.TheResult == null)
{
    <h4>Are you sure you want to remove this event?</h4>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Event.EventName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Event.EventName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Event.Description)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Event.Description)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Event.Time)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Event.Time)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to My Bookmark", "Index")
            </div>
        }
    </div>
}
